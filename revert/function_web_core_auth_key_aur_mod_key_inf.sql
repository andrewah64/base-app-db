drop function if exists web_core_auth_key_aur_mod.key_inf(refcursor, p_tnt_id bigint, p_aur_id bigint, p_aauk_nm text, p_aauk_enabled boolean, p_dbrl_id bigint, p_offset integer, p_limit integer);

drop function if exists web_core_auth_key_aur_mod.key_inf(refcursor, p_tnt_id bigint, p_aauk_id bigint);
